public class string {
    public static void main(String[] args){
    // String name = new String();// we are creating a object of string but have can create it directly by
    String name = "navin"; //it will create object for you
    name = name +"reddy"; //ypu are not changing the existing string it is creating a new object which will have a new daya as navin reddy so that  means im changing the address of variable not changing the data
    System.out.println(name);
    }
}
